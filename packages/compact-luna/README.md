# compact-luna

Adds `/compact-luna [instructions]`, which runs Pi's standard session compaction with `openai-codex/gpt-5.6-luna` at `xhigh` reasoning without changing the active session model.

Install through the root repository package. See [the root README](../../README.md) for configuration.
