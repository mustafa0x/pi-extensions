# summarize-luna

Adds `/summarize-luna [instructions]`, `/summary-luna-show`, and `/summary-luna-copy`. Summaries use `openai-codex/gpt-5.6-luna` at `xhigh`, persist in the session, and do not compact or change the active session model.

Install through the root repository package. See [the root README](../../README.md).
